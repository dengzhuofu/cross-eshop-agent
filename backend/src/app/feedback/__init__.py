"""M10 反馈-分诊-沉淀闭环。

用户对 agent 产物（客服草稿/Listing 文案/研究结论）的 👍/👎 反馈经本模块：
1. 分诊（triage）：确定性规则分类打底（零网络、可测试），LLM 只做归因增强——
   类别必须落在固定 taxonomy 内，越界即弃用回退规则结果（LLM 只提议，代码硬保证）；
2. 沉淀路由（sinks）：按类别把改进信号送到合适组件位置——
   - 知识缺口   → 候选知识条目（status=candidate，人工审批后才进检索池）
   - 检索未命中 → 黄金查询候选集（.localdata/feedback_golden.jsonl，喂 RAG 评估）
   - 幻觉/违禁/时效冲突/数据矛盾 → bad_cases 隔离（复用 M7 分类法与处置闭环）
   - 全部负反馈 → 长期记忆经验条目（复用 M4 记忆管线 + 投毒脱敏）

反馈文本本身是不可信输入：进任何沉淀通道前先过 scrub_untrusted（与 M7 detector
共享同一组正则）——用户的吐槽里藏注入指令也不会被当成数据执行。
"""
